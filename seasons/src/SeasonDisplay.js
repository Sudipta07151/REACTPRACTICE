import React from 'react';

const SeasonDisplay = (props) => {
    return (
        <div>
            <h1>SeasonDisplay</h1>
            <p>Latitude:{props.lat}</p>
        </div>
    );
}

export default SeasonDisplay;